# Sentinel Dashboard 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# ==================== 构建配置 ====================
# 是否使用中国镜像加速（true/false）
USE_CHINA_MIRROR=true

# ==================== 日志配置 ====================
# 日志级别：TRACE, DEBUG, INFO, WARN, ERROR
# 调试时可设置为 DEBUG，生产环境建议使用 INFO 或 WARN
LOG_LEVEL=INFO

# ==================== 认证配置 ====================
# ⚠️ 认证功能已强制启用，无法禁用（安全要求）

# Dashboard Web UI 登录凭据（必需修改）
DASHBOARD_USERNAME=sentinel
DASHBOARD_PASSWORD=sentinel

# 客户端连接 Dashboard 的应用密钥（生产环境必需）
# 客户端必须提供此密钥才能连接到 Dashboard
AUTH_APP_SECRET=your_secret_key_here

# ==================== Nacos 配置 ====================
# Nacos 版本支持：2.2.0 ~ 3.x
# 测试环境：2.3.0（本地和 CI）
# 生产推荐：2.4.3（最新稳定版）
# ⚠️ 不支持 1.x 和 2.0.x/2.1.x 版本（安全性不足）

# Nacos 服务器地址（可选，默认 nacos:8848）
# NACOS_SERVER_ADDR=nacos:8848

# Nacos 命名空间 ID（可选，默认为空字符串，对应 public 命名空间）
# ⚠️ 重要：public 命名空间的 ID 是空字符串，不是 "public"
# - 使用 public 命名空间：保持为空或不设置
# - 使用自定义命名空间：填写命名空间的 UUID（在 Nacos 控制台查看）
# NACOS_NAMESPACE=

# Nacos 分组（可选，默认 DEFAULT_GROUP）
# NACOS_GROUP=DEFAULT_GROUP

# Nacos 认证（必需，默认 nacos/nacos）
# ⚠️ 生产环境必须修改默认密码
NACOS_USERNAME=nacos
NACOS_PASSWORD=nacos

# ==================== 示例配置 ====================

# 开发环境 - 使用默认配置 + DEBUG 日志
# LOG_LEVEL=DEBUG
# DASHBOARD_USERNAME=sentinel
# DASHBOARD_PASSWORD=sentinel
# NACOS_USERNAME=nacos
# NACOS_PASSWORD=nacos

# 生产环境 - 强化所有安全配置（必需）
# LOG_LEVEL=WARN
# DASHBOARD_USERNAME=admin
# DASHBOARD_PASSWORD=StrongPassword2024!
# AUTH_APP_SECRET=prod_secret_2024_ComplexKey
# NACOS_USERNAME=sentinel_admin
# NACOS_PASSWORD=NacosStrongPass2024!
# NACOS_NAMESPACE=sentinel-prod
# NACOS_GROUP=SENTINEL_GROUP

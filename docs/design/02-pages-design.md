# Sentinel Dashboard 前端页面设计

> 版本：1.0
> 更新日期：2025-12-05

---

## 1. 页面总览

基于旧版 Sentinel Dashboard 功能，前端需要实现以下页面：

| 页面     | 路径                                  | 说明                   | 状态        |
| -------- | ------------------------------------- | ---------------------- | ----------- |
| 登录     | `/auth/sign-in`                       | 用户登录               | ✅ 完成     |
| 首页     | `/dashboard`                          | 应用列表总览           | ✅ 基础完成 |
| 簇点链路 | `/dashboard/apps/:app/identity`       | 资源调用链路           | ✅ 完成     |
| 实时监控 | `/dashboard/apps/:app/metric`         | 实时 QPS 监控          | ✅ 完成     |
| 流控规则 | `/dashboard/apps/:app/flow`           | 流控规则 CRUD          | ✅ 基础完成 |
| 降级规则 | `/dashboard/apps/:app/degrade`        | 熔断降级规则           | ✅ 基础完成 |
| 热点规则 | `/dashboard/apps/:app/param-flow`     | 热点参数规则           | ✅ 基础完成 |
| 系统规则 | `/dashboard/apps/:app/system`         | 系统保护规则           | ✅ 完成     |
| 授权规则 | `/dashboard/apps/:app/authority`      | 黑白名单规则           | ✅ 基础完成 |
| 机器列表 | `/dashboard/apps/:app/instances`      | 机器管理               | ✅ 基础完成 |
| 集群流控 | `/dashboard/cluster/server`           | 全局 Token Server 管理 | ✅ 完成     |
| 集群流控 | `/dashboard/cluster/client`           | 全局 Token Client 管理 | ✅ 完成     |
| 集群流控 | `/dashboard/apps/:app/cluster/server` | 应用级 Token Server    | ✅ 完成     |
| 集群流控 | `/dashboard/apps/:app/cluster/client` | 应用级 Token Client    | ✅ 完成     |

---

## 2. 侧边栏导航设计

### 2.1 导航结构

```
┌─────────────────────────────┐
│ 🏠 首页                     │
├──────────────────────────────┤
│ ▼ 选择服务              │  <- 服务选择器（可搜索）
├──────────────────────────────┤
│ 服务功能                   │  <- 选择服务后显示
│   ├─ 📊 实时监控            │
│   ├─ 🔗 簇点链路            │
│   ├─ 🚦 流控规则            │
│   ├─ ⚡ 熔断规则            │
│   ├─ 🔥 热点规则            │
│   ├─ 🖥️ 系统规则            │
│   ├─ 🔐 授权规则            │
│   └─ 💻 实例列表            │
├──────────────────────────────┤
│ 集群管理                   │  <- 全局级集群管理
│   ├─ Token Server 列表   │
│   └─ Token Client 列表   │
└─────────────────────────────┘
```

### 2.2 导航菜单配置

```typescript
// 应用级别菜单（选中应用后显示）
const appMenuItems = [
  { key: "metric", title: "实时监控", icon: "chart-line", path: "/metric" },
  { key: "identity", title: "簇点链路", icon: "sitemap", path: "/identity" },
  { key: "flow", title: "流控规则", icon: "funnel", path: "/flow" },
  { key: "degrade", title: "熔断规则", icon: "shield-exclamation", path: "/degrade" },
  { key: "param-flow", title: "热点规则", icon: "fire", path: "/param-flow" },
  { key: "system", title: "系统规则", icon: "cog", path: "/system" },
  { key: "authority", title: "授权规则", icon: "key", path: "/authority" },
  { key: "instances", title: "实例列表", icon: "desktop", path: "/instances" },
];

// 全局级集群管理菜单（不依赖应用选择）
const clusterMenuItems = [
  { key: "cluster-server", title: "Token Server 列表", icon: "hard-drives", path: "/cluster/server" },
  { key: "cluster-client", title: "Token Client 列表", icon: "laptop", path: "/cluster/client" },
];
```

---

## 3. 页面详细设计

### 3.1 簇点链路页面（核心新增页面）

**功能描述**：
显示应用的所有资源调用链路，展示实时数据，支持快速添加规则。

**页面布局**：

```
┌────────────────────────────────────────────────────────────────────┐
│ {应用名}                              [树状视图] [列表视图] │
├────────────────────────────────────────────────────────────────────┤
│ 簇点链路                                                           │
│ ┌──────────────┐  ┌──────────────────┐                 ┌─────────┐│
│ │ 机器: [下拉] │  │ 关键字: [输入框] │                 │ [刷新]  ││
│ └──────────────┘  └──────────────────┘                 └─────────┘│
├────────────────────────────────────────────────────────────────────┤
│ 资源名        │ 通过QPS │ 拒绝QPS │ 线程数 │ 平均RT │ 分钟通过/拒绝 │ 操作 │
├───────────────┼─────────┼─────────┼────────┼────────┼───────────────┼──────┤
│ ▶ resource_1  │   100   │    5    │   10   │  32ms  │   5000/200    │ [+]  │
│   └─ child_1  │    50   │    0    │    5   │  20ms  │   2500/0      │ [+]  │
│ resource_2    │    80   │    2    │    8   │  25ms  │   4000/100    │ [+]  │
├────────────────────────────────────────────────────────────────────┤
│ 共 136 条记录  每页 [16] 条                    « < 1 2 3 4 5 > »   │
└────────────────────────────────────────────────────────────────────┘
```

**操作按钮展开**：

```
[+流控] [+熔断] [+热点] [+授权]
```

**API**：

- `GET /resource/instanceResource.json?ip=&port=&type=&searchKey=`

**组件拆分**：

```
pages/dashboard/identity/
├── index.tsx              # 页面入口
├── components/
│   ├── ResourceTable.tsx  # 资源表格（支持树状/列表）
│   ├── ResourceRow.tsx    # 资源行（可展开子节点）
│   ├── InstanceSelector.tsx # 机器选择器
│   ├── ViewToggle.tsx     # 视图切换按钮
│   └── QuickRuleDialog.tsx # 快速添加规则对话框
```

---

### 3.2 实时监控页面（需完善图表）

**当前状态**：只有表格展示，缺少图表

**目标设计**：

```
┌────────────────────────────────────────────────────────────────────┐
│ {应用名} - 实时监控                                                │
├────────────────────────────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────────────────────────┐   │
│ │                     QPS 趋势图 (折线图)                       │   │
│ │  ━━━ 通过QPS   ━━━ 拒绝QPS   ━━━ 异常QPS                     │   │
│ │      ↑                                                       │   │
│ │  100 ┤     ╭─╮                                              │   │
│ │   80 ┤   ╭─╯ ╰─╮                                            │   │
│ │   60 ┤ ╭─╯     ╰─╮                                          │   │
│ │   40 ┤─╯         ╰─────                                     │   │
│ │    0 ┴──────────────────────────────────────→ 时间          │   │
│ └──────────────────────────────────────────────────────────────┘   │
│                                                                     │
│ 资源列表                                       ┌─────────┐         │
│                                                │ [刷新]  │         │
├────────────────────────────────────────────────────────────────────┤
│ 资源名        │ 通过QPS │ 拒绝QPS │ 线程数 │ 平均RT │               │
├───────────────┼─────────┼─────────┼────────┼────────┼               │
│ resource_1    │   100   │    5    │   10   │  32ms  │               │
│ resource_2    │    80   │    2    │    8   │  25ms  │               │
└────────────────────────────────────────────────────────────────────┘
```

**组件拆分**：

```
pages/dashboard/metric/
├── index.tsx              # 页面入口
├── components/
│   ├── MetricChart.tsx    # QPS 趋势图
│   ├── MetricTable.tsx    # 资源数据表格
│   └── TimeRangeSelector.tsx # 时间范围选择
```

---

### 3.3 规则列表页面（通用模式）

所有规则页面共用类似布局：

```
┌────────────────────────────────────────────────────────────────────┐
│ {规则类型}规则                                      [+ 新增规则]   │
├────────────────────────────────────────────────────────────────────┤
│ 应用: {应用名}                                                     │
│ ┌──────────────────┐                                               │
│ │ 关键字: [搜索框] │                                               │
│ └──────────────────┘                                               │
├────────────────────────────────────────────────────────────────────┤
│ 资源名 │ ... 规则特定字段 ... │ 操作                               │
├────────┼──────────────────────┼─────────────────────────────────────┤
│ xxx    │ ...                  │ [编辑] [删除]                       │
├────────────────────────────────────────────────────────────────────┤
│ 共 X 条记录  每页 [16] 条                         « < 1 2 3 > »    │
└────────────────────────────────────────────────────────────────────┘
```

**通用组件**：

```
components/dashboard/rules/
├── RuleListLayout.tsx     # 规则列表通用布局
├── RuleSearchBar.tsx      # 搜索栏
├── Pagination.tsx         # 分页组件
├── ConfirmDeleteDialog.tsx # 删除确认框
│
├── FlowRuleForm.tsx       # 流控规则表单
├── DegradeRuleForm.tsx    # 降级规则表单
├── ParamFlowRuleForm.tsx  # 热点规则表单
├── SystemRuleForm.tsx     # 系统规则表单
└── AuthorityRuleForm.tsx  # 授权规则表单
```

---

### 3.4 规则字段对照表

#### 流控规则 (FlowRule)

| 字段              | 显示名   | 类型    | 说明                          |
| ----------------- | -------- | ------- | ----------------------------- |
| resource          | 资源名   | string  | 规则作用的资源                |
| limitApp          | 来源应用 | string  | 来源限制，default 表示不限    |
| grade             | 阈值类型 | 0/1     | 0:线程数, 1:QPS               |
| count             | 阈值     | number  | 限流阈值                      |
| strategy          | 流控模式 | 0/1/2   | 0:直接, 1:关联, 2:链路        |
| refResource       | 关联资源 | string  | 关联/链路模式时的资源         |
| controlBehavior   | 流控效果 | 0/1/2   | 0:快速失败, 1:Warm Up, 2:排队 |
| warmUpPeriodSec   | 预热时长 | number  | Warm Up 预热时间(秒)          |
| maxQueueingTimeMs | 超时时间 | number  | 排队等待超时(毫秒)            |
| clusterMode       | 集群模式 | boolean | 是否集群限流                  |

#### 降级规则 (DegradeRule)

| 字段               | 显示名     | 类型   | 说明                               |
| ------------------ | ---------- | ------ | ---------------------------------- |
| resource           | 资源名     | string | 规则作用的资源                     |
| grade              | 熔断策略   | 0/1/2  | 0:慢调用比例, 1:异常比例, 2:异常数 |
| count              | 阈值       | number | 策略对应的阈值                     |
| timeWindow         | 熔断时长   | number | 熔断恢复时间(秒)                   |
| minRequestAmount   | 最小请求数 | number | 触发熔断的最小请求数               |
| statIntervalMs     | 统计时长   | number | 统计窗口(毫秒)                     |
| slowRatioThreshold | 慢调用比例 | number | 慢调用比例阈值                     |

#### 热点规则 (ParamFlowRule)

| 字段              | 显示名   | 类型    | 说明             |
| ----------------- | -------- | ------- | ---------------- |
| resource          | 资源名   | string  | 规则作用的资源   |
| paramIdx          | 参数索引 | number  | 热点参数索引位置 |
| grade             | 限流模式 | 0/1     | 0:线程数, 1:QPS  |
| count             | 单机阈值 | number  | 限流阈值         |
| durationInSec     | 统计窗口 | number  | 统计窗口时长(秒) |
| paramFlowItemList | 例外项   | array   | 参数例外配置     |
| clusterMode       | 集群模式 | boolean | 是否集群限流     |

#### 系统规则 (SystemRule)

| 字段              | 显示名          | 类型   | 说明             |
| ----------------- | --------------- | ------ | ---------------- |
| highestSystemLoad | 最高 Load       | number | 系统最高负载     |
| highestCpuUsage   | 最高 CPU 使用率 | number | CPU 使用率上限   |
| avgRt             | 平均 RT         | number | 所有入口平均 RT  |
| maxThread         | 最大线程数      | number | 入口最大并发数   |
| qps               | 入口 QPS        | number | 所有入口流量 QPS |

#### 授权规则 (AuthorityRule)

| 字段     | 显示名   | 类型   | 说明                   |
| -------- | -------- | ------ | ---------------------- |
| resource | 资源名   | string | 规则作用的资源         |
| limitApp | 流控应用 | string | 调用方应用名(逗号分隔) |
| strategy | 授权类型 | 0/1    | 0:白名单, 1:黑名单     |

---

## 4. 通用组件设计

### 4.1 表格组件增强

```typescript
// 通用表格 Props
interface DataTableProps<T> {
  data: T[];
  columns: Column<T>[];
  loading?: boolean;
  // 分页
  pagination?: {
    current: number;
    pageSize: number;
    total: number;
    onChange: (page: number, size: number) => void;
  };
  // 搜索
  searchable?: boolean;
  searchPlaceholder?: string;
  onSearch?: (keyword: string) => void;
  // 树形展示
  treeMode?: boolean;
  childrenKey?: string;
  // 行操作
  rowActions?: (row: T) => React.ReactNode;
}
```

### 4.2 规则表单对话框

```typescript
// 通用规则对话框 Props
interface RuleDialogProps<T> {
  open: boolean;
  onClose: () => void;
  mode: "create" | "edit";
  initialData?: T;
  onSubmit: (data: T) => Promise<void>;
  title: string;
}
```

### 4.3 机器选择器

```typescript
// 机器选择器 Props
interface InstanceSelectorProps {
  app: string;
  value?: string; // "ip:port"
  onChange: (value: string) => void;
}
```

---

## 5. API 设计

### 5.1 新增 API 封装

```typescript
// lib/api/resource.ts - 簇点链路 API
export const resourceApi = {
  // 获取机器资源链路
  getInstanceResource: (ip: string, port: number, type?: string, searchKey?: string) =>
    apiClient.get<ClusterNode[]>("/resource/instanceResource.json", {
      params: { ip, port, type, searchKey },
    }),
};

// lib/api/metric.ts - 监控数据 API (增强)
export const metricApi = {
  // 获取实时监控数据
  queryMetric: (app: string, resource: string, startTime: number, endTime: number) =>
    apiClient.get<MetricData[]>("/metric/queryByAppAndResource.json", {
      params: { app, identity: resource, startTime, endTime },
    }),

  // 获取 Top 资源
  queryTopResource: (app: string, pageIndex: number, pageSize: number, desc: boolean) =>
    apiClient.get("/metric/queryTopResourceMetric.json", {
      params: { app, pageIndex, pageSize, desc },
    }),
};
```

### 5.2 Hooks 设计

```typescript
// hooks/api/use-resource.ts
export function useResources(app: string, instance: { ip: string; port: number }) {
  return useQuery({
    queryKey: ["resources", app, instance.ip, instance.port],
    queryFn: () => resourceApi.getInstanceResource(instance.ip, instance.port),
    enabled: !!app && !!instance.ip,
    refetchInterval: 10000, // 10秒刷新一次
  });
}

// hooks/api/use-metric.ts (增强)
export function useMetricHistory(app: string, resource: string, timeRange: [number, number]) {
  return useQuery({
    queryKey: ["metric-history", app, resource, timeRange],
    queryFn: () => metricApi.queryMetric(app, resource, timeRange[0], timeRange[1]),
    enabled: !!app && !!resource,
  });
}
```

---

## 6. 实现优先级

### Phase 1 - 核心功能完善

1. ✅ TypeScript 错误修复
2. ❌ 簇点链路页面（新增）
3. 🔄 系统规则编辑功能

### Phase 2 - 用户体验提升

4. 🔄 列表页分页功能
5. 🔄 列表页搜索过滤
6. 🔄 实时监控图表

### Phase 3 - 高级功能

7. ❌ 集群流控完善
8. ❌ 批量操作
9. ❌ 数据导入导出

---

## 7. 组件目录结构（最终）

```
src/
├── components/
│   ├── ui/                    # 基础 UI 组件
│   │   ├── DataTable.tsx      # 通用数据表格
│   │   ├── Pagination.tsx     # 分页组件
│   │   ├── SearchInput.tsx    # 搜索输入框
│   │   └── ConfirmDialog.tsx  # 确认对话框
│   │
│   └── dashboard/
│       ├── layout/            # 布局组件
│       │   ├── Layout.tsx
│       │   ├── Sidebar.tsx
│       │   └── AppMenu.tsx
│       │
│       ├── identity/          # 簇点链路组件
│       │   ├── ResourceTable.tsx
│       │   ├── ResourceTreeTable.tsx
│       │   ├── InstanceSelector.tsx
│       │   └── QuickRuleButtons.tsx
│       │
│       ├── metric/            # 监控组件
│       │   ├── MetricChart.tsx
│       │   └── MetricTable.tsx
│       │
│       └── rules/             # 规则相关组件
│           ├── RuleListLayout.tsx
│           ├── FlowRuleForm.tsx
│           ├── DegradeRuleForm.tsx
│           ├── ParamFlowRuleForm.tsx
│           ├── SystemRuleForm.tsx
│           └── AuthorityRuleForm.tsx
│
├── pages/
│   └── dashboard/
│       ├── overview.tsx       # 首页
│       ├── identity/          # 簇点链路
│       │   └── index.tsx
│       ├── metric/            # 实时监控
│       │   └── index.tsx
│       ├── flow/              # 流控规则
│       │   ├── list.tsx
│       │   ├── create.tsx
│       │   └── edit.tsx
│       ├── degrade/           # 降级规则
│       ├── param-flow/        # 热点规则
│       ├── system/            # 系统规则
│       ├── authority/         # 授权规则
│       ├── cluster/           # 集群流控
│       └── instances/          # 机器列表
│
├── hooks/
│   └── api/
│       ├── use-app.ts
│       ├── use-instance.ts
│       ├── use-resource.ts    # 新增
│       ├── use-metric.ts      # 增强
│       ├── use-flow-rule.ts
│       ├── use-degrade-rule.ts
│       ├── use-param-flow-rule.ts
│       ├── use-system-rule.ts
│       ├── use-authority-rule.ts
│       └── use-cluster.ts
│
├── lib/
│   └── api/
│       ├── client.ts
│       ├── app.ts
│       ├── instance.ts
│       ├── resource.ts        # 新增
│       ├── metric.ts          # 增强
│       ├── flow-rule.ts
│       ├── degrade-rule.ts
│       ├── param-flow-rule.ts
│       ├── system-rule.ts
│       ├── authority-rule.ts
│       └── cluster.ts
│
└── types/
    ├── sentinel.ts            # 增强 ClusterNode 类型
    ├── rule.ts
    └── cluster.ts
```

---

## 8. 集群流控页面设计

### 8.1 全局 Token Server 列表

**路径**：`/dashboard/cluster/server`

**设计理念**：

- 展示独立部署的 Token Server（`embedded=false`）
- 这些是专门的令牌服务器，不属于任何业务应用
- K8s 环境下可以独立扩容（增加 pod 数量）

**页面结构**：

```
┌──────────────────────────────────────────────────────────────────┐
│ Token Server 列表                                [刷新]          │
│ 独立部署的 Token Server（K8s 环境下可独立扩容）                │
├──────────────────────────────────────────────────────────────────┤
│ 应用                 │ IP         │ Token Server │ 服务命名空间  │
│                     │            │ 端口         │               │
├──────────────────────────────────────────────────────────────────┤
│ sentinel-token-     │ 172.18.0.5 │ 18730       │ app1,app2,app3│
│ server              │            │             │               │
│ (独立模式)          │            │             │ 客户端: 5      │
└──────────────────────────────────────────────────────────────────┘
```

**API**：

```typescript
GET /cluster/server_list  // 不传 app 参数
返回：List<AppClusterServerStateWrapVO>（过滤 embedded=false）
```

**数据展示**：

- 应用名：`state.appName`
- IP：`ip`
- 端口：`port`
- Token Server 端口：`state.port`
- 模式：`state.embedded ? '嵌入模式' : '独立模式'`
- 服务命名空间：`state.namespaceSet.join(', ')`
- 客户端数量：`state.connection.reduce((sum, c) => sum + c.connectedCount, 0)`

### 8.2 应用级 Token Server 列表

**路径**：`/dashboard/apps/:app/cluster/server`

**设计理念**：

- 展示指定应用内的 Token Server（包括嵌入模式）
- 用于应用内部的集群管理
- 某个应用的实例被配置为 Token Server，为该应用的其他实例提供服务

**页面结构**：

```
┌──────────────────────────────────────────────────────────────────┐
│ 集群流控 - Token Server                        [刷新] [分配]    │
│ 应用：my-service                                                │
├──────────────────────────────────────────────────────────────────┤
│ Token Server 列表                                               │
├──────────────────────────────────────────────────────────────────┤
│ 地址           │ 端口  │ 状态      │ 客户端数 │ 命名空间         │
├──────────────────────────────────────────────────────────────────┤
│ 10.0.1.5      │ 8719 │ 嵌入模式   │ 3       │ my-service       │
└──────────────────────────────────────────────────────────────────┘
```

**API**：

```typescript
GET /cluster/server_list?app=my-service  // 传 app 参数
返回：List<AppClusterServerStateWrapVO>（包含所有模式）
```

### 8.3 全局 Token Client 列表

**路径**：`/dashboard/cluster/client`

**设计理念**：

- 展示所有连接到 Token Server 的客户端
- 用于监控集群流控的整体状况

**页面结构**：

```
┌──────────────────────────────────────────────────────────────────┐
│ Token Client 列表                                [刷新]          │
│ 全局 Token Client 管理                                          │
├──────────────────────────────────────────────────────────────────┤
│ 应用        │ IP         │ 端口  │ Token Server    │ 超时时间    │
├──────────────────────────────────────────────────────────────────┤
│ app1       │ 10.0.1.10  │ 8719 │ 172.18.0.5:18730│ 20s         │
│ app1       │ 10.0.1.11  │ 8719 │ 172.18.0.5:18730│ 20s         │
│ app2       │ 10.0.2.10  │ 8719 │ 172.18.0.5:18730│ 20s         │
└──────────────────────────────────────────────────────────────────┘
```

**API**：

```typescript
GET /cluster/client_list  // 不传 app 参数
返回：List<AppClusterClientStateWrapVO>
```

### 8.4 设计要点

**区分两种模式**：

1. **独立模式（Standalone）**：

   - 专门的 Token Server 应用（如 sentinel-token-server）
   - 不处理业务逻辑，只负责令牌分发
   - K8s 环境下可以独立扩容
   - `embedded=false`

2. **嵌入模式（Embedded）**：
   - 业务应用内选一个实例作为 Token Server
   - 该实例既处理业务又提供令牌服务
   - `embedded=true`

**API 设计原则**：

- 全局查询（不传 app）：只返回独立模式的 Token Server
- 按应用查询（传 app）：返回该应用的所有 Token Server（包括嵌入模式）

**前后端适配**：

- 前端类型定义直接匹配后端 `AppClusterServerStateWrapVO`
- 不做额外的数据转换，保持架构简洁
- 前端从 `state` 嵌套对象中提取需要展示的信息

---

## 9. 下一步行动

1. **确认设计** - 请确认以上设计是否符合预期
2. **开始实现** - 按优先级顺序实现功能
3. **测试验证** - 每完成一个功能模块进行测试
